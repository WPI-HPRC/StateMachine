#include "Context.h"

/**
 * @brief Initializes the context structure with default or provided values.
 *
 * This function is responsible for initializing the `Context` object used throughout
 * the state machine. It sets up any necessary sensor values, default parameters, or
 * any other state-related data required to start the operation.
 *
 * @param ctx A pointer to the `Context` structure that will be initialized.

 * @note This function should be called before transitioning into any state that depends
 *       on the context being properly set up.
 */
void initializeContext(Context* ctx) {
    // Code to initialize context...
}